<!DOCTYPE html>
<html>
  <%- include('_header.ejs') %>
  <body>
    <%- include('_bold_nav_bar.ejs') %>

      <div class="intro-container photo-text">
        <form action="edit_photo" method="post" enctype="multipart/form-data" >      
        <h1>Photo</h1>
        <p>Let me see that smile!<br> Add a minimum of 3 pictures</p>  
      </div>  

      <div class="photo-container"> 
        <div class = "photo-wrapper">
          <div class="box">
            <div class="image-preview" style="background-image:url(<%=user.photo[0]%>);"></div>
            <div class="upload-options">
              <label>
                <input name="photo" type="file" class="image-upload" accept="image/*" required />
              </label>
            </div>
          </div>

          <div class="box">
            <div class="image-preview" style="background-image:url(<%=user.photo[1]%>);"></div>
            <div class="upload-options">
              <label>
                <input name="photo" type="file" class="image-upload" accept="image/*"   />
              </label>
            </div>
          </div>

          <div class="box">
            <div class="image-preview" style="background-image:url(<%=user.photo[2]%>);"></div>
            <div class="upload-options">
              <label>
                <input name="photo" type="file" class="image-upload" accept="image/*" />
              </label>
            </div>
          </div>

          <div class="box">
            <div class="image-preview" style="background-image:url(<%=user.photo[3]%>);"></div>
            <div class="upload-options">
              <label>
                <input type="file" class="image-upload" accept="image/*" />
              </label>
            </div>
          </div>

          <div class="box">
            <div class="image-preview" style="background-image:url(<%=user.photo[4]%>);"></div>
            <div class="upload-options">
              <label>
                <input type="file" class="image-upload" accept="image/*" />
              </label>
            </div>
          </div>

          <div class="box">
            <div class="image-preview" style="background-image:url(<%=user.photo[5]%>);"></div>
            <div class="upload-options">
              <label>
                <input type="file" class="image-upload" accept="image/*" />
              </label>
            </div>
          </div>
        </div>
        <div class = "bio-container">
          <p>Add a bio to compliment <br> those amazing photos!</p>
          <textarea id="bio" name="bio">Enter here..</textarea>
        </div>
      </div>

      <input type="hidden" name="registerStep" value="done">
        <div class="nextBtn-wrapper">
          <button class="nextBtn">Next</button>
        </div>
      </form> 


  <script type="text/javascript" src="/photo.js"></script>
  <script>
    $("#bio").click(function (e) {
      $("#bio").val("");
    });
  </script>
    <script>
      heightCalculation(".photo-container",320)
    </script>
  </body> 
  
</html>